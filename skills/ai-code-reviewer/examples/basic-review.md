# Basic Review Example
Input: Review this function for correctness and edge cases.

```python
def divide(a, b):
    return a / b
```

Expected direction: identify division-by-zero behavior as a candidate and avoid inventing the intended contract.
